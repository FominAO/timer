import ClockRound from "./clock"

export default class ClockSetup {
  constructor() {
    this.form = `<form>
    
    </form>`
  }
  start() {

  }
}



const timer = new ClockRound(goal, color, lineWidth, radius);